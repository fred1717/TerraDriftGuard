EXECUTION: 58001ba0-b39d-a20e-61f5-7131eba12171_1a64ab64-159d-7139-db2c-8c15899e0ed0
STATUS:    SUCCEEDED
STARTED:   2026-03-04T10:41:14.888000+01:00
STOPPED:   2026-03-04T10:41:29.334000+01:00

--- INPUT EVENT ---
Resource:    terradriftguard-public-read-test
Type:        AWS::S3::Bucket
Config Rule: s3-bucket-public-read-prohibited
Region:      us-east-1
Severity:    HIGH
Annotation:  The S3 bucket policy allows public read access.
Previous:    COMPLIANT
Detected At: 2026-03-04T09:41:07.374Z

--- ENRICHMENT ---
Config Status:   OK
Prior Incidents: 0

--- BEDROCK ANALYSIS ---
SUMMARY:
S3 bucket 'terradriftguard-public-read-test' has been configured with a bucket policy that allows public read access, violating the s3-bucket-public-read-prohibited Config rule.

RISK ASSESSMENT:
HIGH - This creates a critical security vulnerability where sensitive data stored in the bucket could be accessed by unauthorized users on the internet, potentially leading to data breaches, compliance violations, and exposure of confidential information.

LIKELY CAUSE:
This was likely intentional, possibly for testing purposes given the bucket name contains 'public-read-test', but may have been left in this insecure state accidentally after debugging or development work was completed.

REMEDIATION:
Remove or modify the S3 bucket policy to eliminate public read access by removing any statements that grant 's3:GetObject' or 's3:GetObjectVersion' permissions to the 'Principal' value of '*' or AWS account principals that allow public access.

--- TERRAFORM SNIPPET ---
resource "aws_s3_bucket" "terradriftguard_public_read_test" {
  bucket = "terradriftguard-public-read-test"
  
  tags = {
    Environment = "test"
    Purpose = "drift-detection"
  }
}

# Explicitly block all public access
resource "aws_s3_bucket_public_access_block" "terradriftguard_public_read_test" {
  bucket = aws_s3_bucket.terradriftguard_public_read_test.id
  
  # Block public ACLs and bucket policies
  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

# Optional: Define a restrictive bucket policy for internal access only
resource "aws_s3_bucket_policy" "terradriftguard_public_read_test" {
  bucket = aws_s3_bucket.terradriftguard_public_read_test.id
  
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid       = "DenyPublicAccess"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:*"
        Resource = [
          aws_s3_bucket.terradriftguard_public_read_test.arn,
          "${aws_s3_bucket.terradriftguard_public_read_test.arn}/*"
        ]
        Condition = {
          Bool = {
            "aws:PrincipalIsAWSService" = "false"
          }
        }
      }
    ]
  })
}

--- GENERATED TERRAFORM FILE ---
# Auto-generated by TerraDriftGuard
# Drift remediation for: s3-bucket-public-read-prohibited
# Resource: AWS::S3::Bucket / terradriftguard-public-read-test
# Region: us-east-1
# Generated: 2026-03-04T09:41:07.374Z
#
# Review this file before applying. This is a proposed fix,
# not an automatic remediation.

terraform {
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "us-east-1"
}

resource "aws_s3_bucket" "terradriftguard_public_read_test" {
  bucket = "terradriftguard-public-read-test"
  
  tags = {
    Environment = "test"
    Purpose = "drift-detection"
  }
}

# Explicitly block all public access
resource "aws_s3_bucket_public_access_block" "terradriftguard_public_read_test" {
  bucket = aws_s3_bucket.terradriftguard_public_read_test.id
  
  # Block public ACLs and bucket policies
  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

# Optional: Define a restrictive bucket policy for internal access only
resource "aws_s3_bucket_policy" "terradriftguard_public_read_test" {
  bucket = aws_s3_bucket.terradriftguard_public_read_test.id
  
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid       = "DenyPublicAccess"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:*"
        Resource = [
          aws_s3_bucket.terradriftguard_public_read_test.arn,
          "${aws_s3_bucket.terradriftguard_public_read_test.arn}/*"
        ]
        Condition = {
          Bool = {
            "aws:PrincipalIsAWSService" = "false"
          }
        }
      }
    ]
  })
}


--- ESCALATION ---
PR URL:   SKIPPED
Branch:   drift-fix/s3-bucket-public-read-prohibited/terradriftguard-public-read-test-20260304094128
Filename: remediate_s3_bucket_public_read_prohibited_terradriftguard_public_read_test.tf
SNS Sent: True
